# dealme

This is a toy repository to practice deploying Sphinx docs to GitHub Pages.

Test with:

```sh
$ uv run pytest
```

Build docs with

```sh
$ uv run just docs
```

View the locally built docs with

```sh
$ uv run just view-docs
```

